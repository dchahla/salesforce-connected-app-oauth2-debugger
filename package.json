{
  "name": "oauth2-salesforce-connector-tester",
  "description": "a minimalist tool for testing and debugging oauth2 SF connected app configurations. run it locally to validate oidc metadata, token requests, and callback flows.",
  "author": "Daniel Chahla",
  "type": "module",
  "main": "index.js",
  "version": "1.0.0",
  "release": {
    "tagFormat": "v${version}z"
  },
  "scripts": {
    "start": "npx tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "oauth2",
    "saleforce",
    "connected app",
    "PKCE",
    "scopes"
  ],
  "license": "ISC",
  "dependencies": {
    "express": "^4.21.2",
    "ts-node": "^10.9.2",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^22.12.0",
    "@types/ws": "^8.5.14",
    "typescript": "^5.7.3"
  }
}
